<header>

  <div class="logo">
    <%= image_tag("logo.png") %>
  </div>

  <nav class="nav">
    <% if current_page?(root_path)%>
      <a href="/jobs" class="current">All Job</a>
    <% else %>
      <a href="/jobs">All Job</a>
    <% Category.all.each do |category| %>
      <%= link_to category.name, jobs_path(category: category.name)%>
    <% end %>
    <% end %>
  </nav>

</header>
